import Stack from './stack'
import Queue from './queue'
//npm run test

test('stack', () => {
  expect(stackSolution()).toEqual(1);
  expect(stackSolution()).toEqual(1);
  expect(stackSolution()).toEqual(1);
  expect(stackSolution()).toEqual(1);
});

function stackSolution(){

}

test('queue', () => {
  expect(queueStack()).toEqual(1);
  expect(queueStack()).toEqual(1);
  expect(queueStack()).toEqual(1);
  expect(queueStack()).toEqual(1);
});

function queueStack(){

}